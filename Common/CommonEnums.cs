namespace Common;

public enum GameDirection
{
    North,
    South
}
    
public enum GameMode
{
    Offline,
    Online
}

public enum GameStatus
{
    Active,
    Paused,
    NonActive,
    NonInitialiezed
}

public enum TeamColor
{
    White = 1,
    Black = 2
}