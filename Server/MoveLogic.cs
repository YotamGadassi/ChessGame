using Common;
using System.Windows;

namespace Server
{
    public static class SoldierMoveLogic
    {
        public static bool IsMoveLegal(bool MoveForward ,Move move)
        {
            Vector diff = move.Diff;

            if (MoveForward)
            {
                if (diff.X == 1 && diff.Y ==)
            }
        }
    }
}
